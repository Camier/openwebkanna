# Minimal dependency set for scripts/extract_smiles_from_images.py
# Core inference + parsing
transformers>=4.57.0,<5
torch>=2.2,<3
Pillow>=10.0
DECIMER>=2.7,<3
opencv-python<4.13

# Optional: speed up/quantization helpers for Transformers generation
# accelerate>=0.35.0

# SMILES validation (required for canonicalization + validity filtering)
# rdkit-pypi currently expects NumPy < 2.0 when paired with this pin.
numpy<2.0
rdkit-pypi==2022.9.5

# MolScribe currently depends on legacy torch constraints and should be
# isolated in a separate environment when needed.

# Optional OCSR backends for scripts/extract_smiles_from_images.py
# pip install molscribe
# pip install DECIMER
# pip install imago
