# MCP Compatibility Copy

This directory is a root compatibility surface for operators.

Canonical edit paths:

- `config/mcp/config.json`
- `config/mcp/config.research.optional.json`

Current compatibility copies in this directory:

- `config.json`
- `config.research.optional.json`

Rule:

- Edit `config/mcp/` first.
- Keep this root copy aligned because MCPO configuration is still exposed at the repo root for operator convenience and existing script defaults.
